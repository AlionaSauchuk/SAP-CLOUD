<core:FragmentDefinition
	xmlns="sap.m"
	xmlns:l="sap.ui.layout"
	xmlns:f="sap.ui.layout.form"
	xmlns:core="sap.ui.core">
	<VBox class="sapUiSmallMargin">
		<f:SimpleForm 
			id="SimpleFormDisplayColumn_oneGroup234"
			editable="false"
			layout="ColumnLayout"
			columnsM="2"
			columnsL="3"
			columnsXL="4"
			>
			<f:content>
				<Label text="{i18n>bookIdLabel}" />
				<Text id="ID" text="{books>bid}" />
				<Label text="{i18n>bookCaptionLabel}" />
				<Text id="captionText" text="{books>caption}" />
				<Label text="{i18n>authorIdLabel}" />
				<Text text="{books>authid}" />	
				<Label text="{i18n>updatedOnLabel}" />
				<Text text="{books>onUpdate}" />	
				<Label text="{i18n>createdOnLabel}" />
				<Text text="{books>onCreate}" />	
			</f:content>
		</f:SimpleForm>
	</VBox>
</core:FragmentDefinition>